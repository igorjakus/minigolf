#include "Sus.h"

#include "Agl.h"
#include <iostream>
#include <fstream>

int * sus::Load(std::string filePath) { //na razie sciezka, potem bedzie wybieralo typ (dziwek, tekstura) i wystarczy nazwa (np. tekstura, white)

	std::ifstream file;
	file.open(filePath);

	return 0;
}